services:
  postgresql_kbo:
    image: postgres:17
    container_name: pg_kbo_dev
    environment:
      POSTGRES_USER: ${KBO_POSTGRES_USER:-kbo}
      POSTGRES_PASSWORD: ${KBO_POSTGRES_PASSWORD:-kbo}
      POSTGRES_DB: kbo
    ports:
      - "${KBO_POSTGRES_PORT:-5436}:5432"
    # NACE list queries need more than Docker's default 64MB /dev/shm
    shm_size: "1gb"
    volumes:
      - postgresql_data_kbo:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${KBO_POSTGRES_USER:-kbo} -d kbo"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgresql_data_kbo:
